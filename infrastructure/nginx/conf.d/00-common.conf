# Upstream for gateway services (Containerized microservice gateway with host fallback)
upstream corporate_gateway {
    server subdomain-gateway:3000 max_fails=2 fail_timeout=5s;
    server host.docker.internal:3000 backup;
    keepalive 16;
}

# Proxy header configuration
proxy_set_header Host $host;
proxy_set_header X-Real-IP $remote_addr;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header X-Forwarded-Host $host;
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection "upgrade";

# Timeout, failover policies, and graceful maintenance interception
proxy_connect_timeout 3s;
proxy_read_timeout 60s;
proxy_send_timeout 60s;
proxy_next_upstream error timeout invalid_header http_502 http_503 http_504;
proxy_intercept_errors on;
error_page 502 503 504 =503 /maintenance.html;
