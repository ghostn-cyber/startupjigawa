{
  "name": "@startupjigawa/database",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "generate": "prisma generate",
    "prisma:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --name init --schema prisma/schema.prisma",
    "seed": "prisma db seed"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.1",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
